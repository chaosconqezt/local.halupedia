## 2024-06-25 - ARIA Labels on Icon-Only Buttons
**Learning:** React components using lucide-react icons often lack accessible labels by default when used as icon-only buttons.
**Action:** Always verify that buttons containing only icons (like Trash2 or RefreshCw) have explicit aria-label attributes for screen reader accessibility.
